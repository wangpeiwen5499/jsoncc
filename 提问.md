输出50个json解析的用例，然后分别利用项目json解析去测试，测试结果有报错就修复，没有就通过。用例，测试报告，修复列表都输出文档。



~~折叠图标展示，展示在大括号｛ 或者中括号[的左边，而不是在key的左边，这样同层级的key渲染会对不齐缩进。~~



~~key对应的value如果是对象，则只需要展示Object图标，不需要数量图标。如果是集合，则需要展示Array图标和数量图标~~



~~集合的渲染有问题，不应该显示元素前面的序号，这样看起来像是key map结构。集合的key渲染展示需要加上双引号。~~



~~Object展示为首字母大写，而不是全大写图标，集合图标同理。~~



~~**"\u003e"**渲染成了由尖括号，应该展示成字符串~~



~~json格式化的输入，输出结果垂直堆叠。对空间的利用不够高效，和json对比一样的水平堆叠，并且竟可能占满屏幕，只需要在屏幕两侧预留竖排的广告位即可~~



~~语言这个选项，根据访问者浏览器的语言设置即可。缺省为英语。时间戳也根据访问者浏览器的时区信息来生成当前日期，和得到时间戳转成的日期。也可以给出按钮让用户选择时区信息。时间戳页面重新设计布局，不能把按钮拉得和页面一样宽，希望更优雅实现。~~



我要json格式化和对比功能进行联动。当json解析输出结果之后，要展示一个按钮“加入对比”。点击之后，会把当前json格式化结果放进对比的一个框之中。当两个框都放满了，就直接跳转到对比页面。对比页面也只需要左侧和右侧的文本输入框，下面的对比框不需要。直接在左右框中进行对比，然后把不同进行高亮显示。



~~对比左侧json，右侧json不要垂直方向堆叠，而是水平方向。~~



json对比的编辑框没有填充满展示部分。并且我发现一个bug，在json格式化添加对比的两段json进行对比，然后点击编辑按钮，就会回到之前旧的json内容的编辑。而不是从格式化加入对比的json。修复